name: Auto Unsigned Build

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate short SHA
        id: set-short-sha
        run: echo "SHORT_SHA=${GITHUB_SHA:0:7}" >> $GITHUB_ENV

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'adopt'
          java-version: 17
          cache: 'gradle'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build Unsigned APKs with Gradle
        run: ./gradlew assembleRelease --warning-mode all

      - name: Upload arm64-v8a Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytdlnis-arm64-v8a-unsigned-${{ env.SHORT_SHA }}
          path: app/build/outputs/apk/release/app-arm64-v8a-release-unsigned.apk

      - name: Upload armeabi-v7a Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytdlnis-armeabi-v7a-unsigned-${{ env.SHORT_SHA }}
          path: app/build/outputs/apk/release/app-armeabi-v7a-release-unsigned.apk

      - name: Upload x86 Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytdlnis-x86-unsigned-${{ env.SHORT_SHA }}
          path: app/build/outputs/apk/release/app-x86-release-unsigned.apk

      - name: Upload x86_64 Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytdlnis-x86_64-unsigned-${{ env.SHORT_SHA }}
          path: app/build/outputs/apk/release/app-x86_64-release-unsigned.apk

      - name: Upload universal Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ytdlnis-universal-unsigned-${{ env.SHORT_SHA }}
          path: app/build/outputs/apk/release/app-universal-release-unsigned.apk
